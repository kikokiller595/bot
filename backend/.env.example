# Configuración del Servidor
PORT=5000
NODE_ENV=development

# MongoDB
MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/loteria?retryWrites=true&w=majority

# JWT Secret (Cambiar en producción por una clave segura)
JWT_SECRET=tu_clave_secreta_super_segura_cambiala_en_produccion

# JWT Expiration
JWT_EXPIRE=30d

# CORS - URLs del Frontend separadas por coma
FRONTEND_URL=http://localhost:3000,https://tu-frontend.netlify.app,https://tu-frontend.vercel.app
